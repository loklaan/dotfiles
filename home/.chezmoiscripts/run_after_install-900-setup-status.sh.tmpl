#!/usr/bin/env bash
set -euo pipefail

IFS=$'\n\t'

# Normalize TMPDIR (strip trailing slash for consistent path construction)
TMPDIR="${TMPDIR:-/tmp}"
TMPDIR="${TMPDIR%/}"

# Source shared logging library and setup session logging
source "${HOME}/.local/lib/bash-logging.sh"
setup_session_logging "$(basename "$0")" "status"

#/ Usage:
#/   run_after_install-900-setup-status.sh
#/
#/ Description:
#/   Checks setup status after chezmoi apply and reminds about
#/   incomplete configuration.
#/
#/ Options:
#/   --help:      Display this help message
usage() { grep '^#/' "$0" | cut -c4-; }

main() {
  local has_bws="false"
  local has_signing="false"

  if [ -f "{{ .bwsTokenPath }}" ]; then
    has_bws="true"
  fi

  {{- if ne "" .signingKey }}
  has_signing="true"
  {{- end }}

  if [ "$has_bws" = "false" ] || [ "$has_signing" = "false" ]; then
    log_warn_step "Checking dotfiles setup"
    if [ "$has_bws" = "false" ]; then
      log_fail "Missing BWS token (npm auth, GitHub tokens)"
    fi
    if [ "$has_signing" = "false" ]; then
      log_fail "Missing signing key (git commit signing)"
    fi
    log_note "Run 'df-setup' for instructions"
  fi
}

main "$@"
