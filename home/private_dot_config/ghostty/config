# Documentation: https://ghostty.org/docs/config

# We manage titles ourselves via init/title.zsh so 'title' is excluded.
# ssh-terminfo auto-installs xterm-ghostty terminfo on remote hosts via infocmp/tic.
# ssh-env falls back to TERM=xterm-256color and propagates COLORTERM=truecolor.
shell-integration-features = cursor,sudo,ssh-terminfo,ssh-env

font-family = "JetbrainsMono Nerd Font Mono"
font-size = 15
adjust-cell-height = "20%"
theme = "Catppuccin Mocha"
macos-titlebar-proxy-icon = hidden
macos-icon = retro
window-padding-x = 20
window-padding-y = 10,0
keybind = shift+enter=text:\n
